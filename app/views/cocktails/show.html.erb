<h1>Cocktails#show</h1>
<p>Name: <%= @cocktail.name %></p>
<p><% @cocktail.doses.each do |d| %>
  <%= Ingredient.find(d.ingredient_id).name %>
  (dose: <%= d.amount %>)
<% end %>
<%= link_to 'add dose', new_cocktail_dose_path(@cocktail)%>
</p>